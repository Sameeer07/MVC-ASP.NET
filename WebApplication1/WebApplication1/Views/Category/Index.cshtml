@model IEnumerable<WebApplication1.Models.Catg>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CatgId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CatgName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.CatgId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.CatgName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Status)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.CatgId }) |
        @Html.ActionLink("Details", "Details", new { id = item.CatgId }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.CatgId })
    </td>
</tr>
}

</table>
